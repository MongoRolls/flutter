# 渴了么 (KeLeME) - 产品需求文档

## 产品定位

**应用名称**: 渴了么 (KeLeME)  
**英文名**: KE LE ME  
**核心定位**: AI 驱动的智能喝水提醒 & 健康管理工具  
**目标平台**: iOS / Android / macOS（Flutter 跨平台）  
**技术栈**: Flutter 3.11+ / Dart / SharedPreferences / flutter_local_notifications  

---

## 产品愿景（用户原始需求）

> App做时可以参考竞品「喝水时间」（界面简约、评价好）。咱们的突出亮点是基于大模型，可以结合天气、本人今日大概规划，预测本日大概喝多少水，然后不是传统只设定一个目标值，同时有个健康小助手对话框，有啥健康饮品问题（包括血液检测报告、体检报告等）都可以往里丢，并且长时间记忆或提醒。另外还想了个社交属性来增强用户粘性：排行榜、积分兑换礼品（水杯、咖啡兑换券等）——有这个物质奖励在，应该有人会坚持下去。

---

## 竞品分析：喝水时间 App

### 新用户初始设置（4 步）
1. **基础信息** — 昵称、性别、运动量、体重
2. **每日目标** — 系统自动生成（2000ml），支持滑块微调
3. **提醒计划** — 起床时间（7:00）、就寝时间（23:00）、间隔（1.5h）
4. **通知权限** — 「允许」/ 「不允许」

### 核心功能页面
- **首页**: 圆环进度（0%）+ 今日记录 + 导航栏
- **提醒设置**: 定时/间隔/进度 3 种模式，午休模式，15min~2h 间隔
- **数据统计**: 日/周/月/年多维度，摄入时间分布条形图
- **个人设置**: 基础信息 + Pro 会员入口

<!-- ### Pro 会员 先不做
- 一次性 29 元永久 / 29 元/年（限时）
- 解锁 20+ 高级功能 + 20+ 可爱角色 -->

---

## MVP 范围（已完成）

### 三大核心页面

#### 1. 新用户设置页（Onboarding）
- Step 1: 昵称 + 性别 + 运动量 + 体重滑块（40-120kg）
- Step 2: 每日目标（体重×35ml 推荐值）+ 大圆环 + 滑块微调（1500-4000ml）
- Step 3: 起床/就寝时间 + 提醒间隔（30min/1h/1.5h/2h）
- Step 4: 通知权限申请

#### 2. 主页面（Home）
- Header: Logo + 通知铃铛 + 设置按钮
- 进度卡片: 问候语 + 日期 + 大圆环 + 百分比 + 「立即喝水打卡」按钮
- 快捷统计: 今日打卡次数 / 连续天数（🔥）/ 待完成次数
- 今日时间表: 时间轴列表（时间|图标|描述|毫升|状态）
- 本月日历: 网格，颜色区分已打卡/未打卡/今天/未来
- 快速打卡抽屉: 150/200/250/350/500ml 快速选择

#### 3. 设置页（Settings）
- 基本设置: 每日目标滑块 + 体重滑块（联动推荐目标）+ 提醒风格（温柔/活泼/严肃）
- 提醒开关: 推送通知开关
- 提醒时间: 起床/就寝时间 + 提醒间隔
- 测试提醒按钮

---

## 设计规范

### 色彩系统（浅色清新风）
| Token | 色值 | 用途 |
|-------|------|------|
| `bgMain` | `#F5F8FF` | 主背景 |
| `blue` | `#29B6F6` | 主强调色 |

> 注：原设计规范为深色科技风，实际实现为浅色风格。

### 字体
- 中文: Noto Sans SC（300/400/500/700）
- 数字/等宽: Space Mono（400/700）

### 圆角
- 卡片: 16px
- 小组件: 10px
- 按钮: 10-12px

---

## 技术架构

### 项目结构
```
ke_le_me/lib/
├── main.dart                  # 入口，路由配置
├── models/
│   └── user_profile.dart      # 用户设置数据模型
├── providers/
│   └── user_provider.dart     # 状态管理（ChangeNotifier）
├── screens/
│   ├── onboarding/            # 新用户引导（4 步）
│   ├── home/                  # 主页面
│   ├── settings/              # 设置页面
│   └── debug/                 # 调试页面
├── services/
│   ├── notification_service.dart  # 本地通知服务
│   └── debug_service.dart         # 调试工具
├── theme/
│   └── app_theme.dart         # 主题色彩/字体定义
└── widgets/
    ├── progress_ring.dart     # 圆环进度条（CustomPaint）
    └── glass_card.dart        # 玻璃卡片容器
```

### 状态管理
`UserProvider`（ChangeNotifier）持有所有运行时状态：
- 用户档案（UserProfile）
- 今日饮水量（`_todayMl`）
- 饮水日志（DrinkLog 列表）

数据流: `UserProvider` → `notifyListeners()` → 页面 `setState()` → UI 重建

### 数据持久化（SharedPreferences）
| Key | 说明 |
|-----|------|
| `user_profile` | 序列化的 UserProfile |
| `today_date` | 当前日期（日期滚动检测） |
| `today_ml` | 今日总饮水量 |
| `today_logs` | 今日饮水记录（JSON 数组）|
| `monthly_hits_{year}_{month}` | 月度打卡数据（用于日历）|
| `history_{date}` | 历史每日总量（保留 365 天，用于连续天数计算）|

### 通知系统
- 使用 `flutter_local_notifications` + `timezone`
- 提前 7 天预排通知，App 启动时重新调度
- 三种提醒风格: 温柔 / 活泼 / 严肃
- 平台权限: Android（POST_NOTIFICATIONS）、iOS/macOS（运行时请求）

### 路由
- `/onboarding` — 新用户引导（首次启动）
- `/home` — 主页面（默认）
- `/settings` — 设置页面
- `/debug` — 调试页面

---

## 当前状态

- ✅ 核心功能: 喝水记录、目标追踪、连续打卡
- ✅ 通知系统: 智能提醒、自定义风格、时区支持
- ✅ 多平台: iOS / Android / macOS / Web
- 🔜 AI 功能: 自定义后端 AI 集成（Gemini 大模型）
- 🔜 社交功能: 排行榜、积分兑换
- 🔜 健康助手: 上传体检报告、长期记忆对话

---

## 后续规划

### Phase 2 — AI Agent 集成
- 接入自定义后端 AI（Gemini `gemini-2.5-flash`）
- AI 智能生成每日饮水计划（结合天气 + 日程）
- 健康小助手对话框（支持上传体检报告）

### Phase 3 — 数据统计
- 近 7 日/月/年饮水统计图表
- 饮品类型分布、健康评分趋势

### Phase 4 — 社交 & 激励
- 用户排行榜、积分系统
- 成就勋章体系（首次/连续 N 天等）
- 积分兑换礼品（水杯、咖啡兑换券）

### Phase 5 — 高级功能
- 饮品百科、手环联动
- Pro 会员系统
