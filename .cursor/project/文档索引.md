# KeLeME 仓库文档索引

本页汇总**与产品研发、协作、部署直接相关**的 Markdown 文档路径与元信息。技能库（`.claude/`、`.agents/` 下大量参考文）不逐条列出。

**索引维护**：`最后索引更新` 见文末；单篇文档的「最近提交」以 `git log -1 --format=%ci -- <路径>` 为准（未纳入版本控制的文件以工作区修改时间为准）。

---

## 1. 根目录与协作入口

| 路径 | 说明 | 最近提交（参考） |
|------|------|------------------|
| [`README.md`](../../README.md) | Monorepo 目录说明 | 见 `git log` |
| [`AGENTS.md`](../../AGENTS.md) | AI/贡献者：命令、架构、风格、测试 | 见 `git log` |
| [`CLAUDE.md`](../../CLAUDE.md) | Claude Code 项目说明与约定 | 见 `git log` |

---

## 2. `.cursor/project`（运维与产品，中文主文档区）

| 路径 | 说明 | 最近提交（参考） |
|------|------|------------------|
| [`README.md`](README.md) | 本区总索引与能力摘要 | 见 `git log` |
| [`文档索引.md`](文档索引.md) | 本文：全库文档目录与元信息 | 2026-03-30 |
| [`后端部署规范.md`](后端部署规范.md) | VPS 默认架构（PM2 + 容器 PG/Redis）、Prisma 约定 | 见 `git log` |
| [`VPS后端部署步骤.md`](VPS后端部署步骤.md) | Step-by-Step 命令清单 | 见 `git log` |
| [`VPS后端部署规划.md`](VPS后端部署规划.md) | 分阶段部署计划 | 见 `git log` |
| [`渴了么产品文档.md`](渴了么产品文档.md) | 产品/信息架构与页面对照 | 见 `git log` |
| [`ssh卡顿优化.md`](ssh卡顿优化.md) | SSH 连接卡顿排查与优化 | 见 `git log` |

**非 Markdown 辅助文件**：`prisma_checksum_fix.sql`、`calculate_prisma_checksums.sh`（见 [`README.md`](README.md) 第 5 节）。

---

## 3. 客户端与后端 README

| 路径 | 说明 | 最近提交（参考） |
|------|------|------------------|
| [`flutter/README.md`](../../flutter/README.md) | Flutter 子项目说明与常用命令 | 见 `git log` |
| [`backend/README.md`](../../backend/README.md) | 后端环境、API、部署与运维 | 见 `git log` |
| [`web/README.md`](../../web/README.md) | Next.js 站点（官网）说明 | 见 `git log` |
| [`web/CLAUDE.md`](../../web/CLAUDE.md) | Web 子目录协作提示 | 见 `git log` |

---

## 4. Flutter 设计说明（节选）

| 路径 | 说明 |
|------|------|
| [`flutter/doc/design-tokens.md`](../../flutter/doc/design-tokens.md) | 设计 Token 说明 |
| [`flutter/doc/modal-migration-inventory.md`](../../flutter/doc/modal-migration-inventory.md) | 弹窗迁移清单（若有） |

---

## 5. OpenSpec（规范驱动开发）

| 路径 | 说明 |
|------|------|
| [`openspec/config.yaml`](../../openspec/config.yaml) | 范围规则（默认含 `flutter/`、`backend/`，默认不含 `web/`） |
| [`openspec/specs/SPEC.md`](../../openspec/specs/SPEC.md) | 规格总览入口 |
| [`openspec/specs/repo-project-docs/spec.md`](../../openspec/specs/repo-project-docs/spec.md) | 项目文档命名与交叉引用要求 |
| [`openspec/specs/*/spec.md`](../../openspec/specs) | 各能力增量规格（如 `home-monthly-streak-calendar`、`health-archive-delete` 等） |
| [`openspec/changes/`](../../openspec/changes) | 进行中的变更提案与归档 |

---

## 6. Cursor 规则（节选）

| 路径 | 说明 |
|------|------|
| [`.cursor/rules/dart-style.mdc`](../rules/dart-style.mdc) | Dart/Flutter 风格 |
| [`.cursor/rules/flutter-ai-toolkit.mdc`](../rules/flutter-ai-toolkit.mdc) | AI Toolkit（按需） |

---

## 元信息

| 字段 | 值 |
|------|-----|
| 最后索引更新 | 2026-03-30（Asia/Shanghai） |
| 维护约定 | 新增「主文档」级说明时，优先放入 `.cursor/project/` 或对应子项目 README，并在此表补一行。 |
